---  
tags:
    - git 
--- 

1. When squash wrong commits -- changging anything in rebase , 
   1. `git reflog` -> to see where the previous head was
   2. `git reset --soft commitid` -> to git reset to that commit -> ``--soft  is super important``
2. https://github.blog/2022-06-30-write-better-commits-build-better-projects/
   1. split the commit via `edit` and then `git reset` individual commit , then `git add -p` and `git commit` 
3. A branch is a narrative -> first commit to refractor, dependencies, setup things 
   1. then one change at a time. 



### split commit in git 
https://stackoverflow.com/questions/6217156/break-a-previous-commit-into-multiple-commits