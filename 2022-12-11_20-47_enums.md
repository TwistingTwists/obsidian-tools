---  
tags:
    - enum 
--- 


## question 
I want to split a map by function. The only way I can see to do it is to use filter/2|reject/2 into an intermediate map, and then split/2 based off of the keys from the intermediate map. Is there a slicker way?

`Enum.group_by |> Enum.map`
