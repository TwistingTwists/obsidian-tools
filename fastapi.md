

path / body / query params 

Router , dependency injection, bg task , 

Database migration - alembic 

python

- gc / memory maangement
- Decorators / 
	- context_managers - class style
		- __enter__ / __exit__ on class 
		- or maybe dectorator style
- try / finally 
	- finally runs event if exception is raised 
- try / except / else / finally 
	- try  - except else - finally 
		- return value in except else --> not returned, finally will be CALLED ALWAYS. so the value is returned always.
- iterators vs generators 
	- CPU efficiency vs memory efficiency 
- immutable / mutable  objects -- list (M) , tuple (IM)
	- set - M , frozen_set - IM
- docstring 
-  ` == ` (comparing value) , `is` (comparing id , memory location)
- dataclass 
- dunder methods 
- memoization method -- @lru_cache  decorator 
- multi threading, multi procesing , asyncio
- 